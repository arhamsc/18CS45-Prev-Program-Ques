/*- User-defined-package:- Java package created by user to categorized classes and interface. 
  These are the packages that are defined by the user.
  Eg program-First we create a directory myPackage (name should be same as the name of the package). 
  Then create the MyClass inside the directory with the first statement being the package names.*/
// Name of the package must be same as the directory
// under which this file is saved
package com.pckgsQ1;//Package Name

public class MyClass
{
    public void getNames(String s)
    {        
        System.out.println(s);        
    }
}
